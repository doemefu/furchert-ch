---
id: "20260522-103903-phase4-oidc-auth-f002"
title: "Phase 4 — Real OIDC dashboard auth (Auth.js v5 → auth.furchert.ch)"
phase: "done"
status: "done"
created_at: "2026-05-22T10:39:03+02:00"
updated_at: "2026-05-22T11:20:00+02:00"
---

## 1. research

**Goal:** Gate `/[locale]/dashboard` behind real OIDC (Auth Code + PKCE) against the
running `homelab-auth-service` (`https://auth.furchert.ch`), expose the `role` claim
server-side, reflect auth state in the Header, and end the IdP session on sign-out —
dashboard *content* stays a minimal placeholder (real shell = Phase 5).

**Branch:** `auth-dashboard` from `dev`. Phase 0/1/2 committed here. **Phase 3
(automation) is on a separate branch and not present** — independent (different
routes); merges cleanly later.

**Current state:**
- `/[locale]/dashboard` 404s (Header/Footer link to it). `Header.tsx` is a client
  component hardcoding `authed = false`.
- `src/middleware.ts` = `next-intl` `createMiddleware(routing)` only; matcher
  excludes `/api`, `_next`, `_vercel`, dotted paths.
- No auth dependency installed (`package.json` deps: next 14.2.23, next-intl
  3.26.4, react 18.3.1). pnpm 9.15.4 via brew-nvm (source nvm per call).
- Prototype `dashboard.jsx` `AuthGate` is a **fake** email/Google/Cloudflare mock
  (`setTimeout` → `onAuth()`). Card styling (lock chip, dotted bg, white card) is
  the only part to keep.

**Repo / sibling inspected:**
- `../auth-service/INTERFACES.md` §1 (OIDC config): discovery
  `https://auth.furchert.ch/.well-known/openid-configuration`, issuer
  `https://auth.furchert.ch`, **PKCE required**, scopes `openid profile email`,
  end-session `https://auth.furchert.ch/connect/logout`; ID-token/UserInfo carry a
  `role` claim (`USER`|`ADMIN`, **not** `ROLE_`-prefixed). §6: registered clients
  are **JDBC-backed** (Flyway V5); `application.yaml` `app.oidc.clients` is
  **bootstrap-only** — `StaticClientSeeder` skips clients that already exist, so on
  an already-running instance the new client must be inserted via `psql`.
- `../auth-service/src/main/resources/application.yaml` — `grafana` client pattern
  (client-secret `${ENV}`, redirect-uris, post-logout-redirect-uris,
  scopes `[openid,profile,email]`). No `furchert-ch` client yet.
- Auth.js v5 docs (context7 `/nextauthjs/next-auth`): `auth.ts` exports
  `{handlers,auth,signIn,signOut}` from `NextAuth(config)`; OIDC provider via
  `{type:'oidc', issuer, clientId, clientSecret}`; route `app/api/auth/
  [...nextauth]/route.ts` re-exports handlers; `jwt`/`session` callbacks for the
  role; `AUTH_SECRET`; `trustHost:true`; split `auth.config.ts` for edge/middleware.

**Assumptions:**
- A1: Auth.js v5 (`next-auth@5.0.0-beta.<N>`, pinned exact) — user-approved. [high]
- A2: Cross-repo auth-service client = prepare+document; user applies + provisions
  secret via SOPS. [high]
- A3: Authed `/dashboard` = minimal placeholder (Phase-5 shell deferred). [high]
- A4: Page-level RSC `auth()` is the authoritative gate; SSG of public pages must be
  preserved (no `auth()` in the shared layout). [high]

**Research summary (3 bullets):**
- Real OIDC contract is fully specified by auth-service §1; only new dep is
  Auth.js v5 (pinned beta).
- The fake `AuthGate` is replaced by a real `signIn('furchert-ch')` button reusing
  only the card styling; tokens stay server-side; Header reflects session via
  client `useSession()` to keep public pages static.
- auth-service client is JDBC-backed → user must `psql`-seed the furchert-ch client
  on the running instance (YAML alone won't seed); secret via SOPS.

---

## 2. plan

> Full approved plan: `~/.claude/plans/fetch-this-design-file-encapsulated-diffie.md`
> (Phase 4 section). Condensed here; section 3 records the plan-review deltas.

**Goal:** `/de`+`/en` `/dashboard` is OIDC-gated (real login round-trip),
`role` available server-side, Header reflects auth, sign-out ends the IdP session;
public pages stay SSG; build/lint/typecheck pass without secrets present.

**Locked decisions:** Auth.js v5 (pinned beta); auth-service change prepared+
documented (user applies); minimal authed placeholder.

**Design decisions:**
- D-A: authoritative gate = server-component `await auth()` in the dashboard RSC
  (Phase-6 admin route handlers re-check `role===ADMIN`). Middleware is not the gate.
- D-B: preserve SSG — no `auth()` in `[locale]/layout.tsx`; Header authed via client
  `<SessionProvider>`+`useSession()`. Only `/[locale]/dashboard` is dynamic.
- D-C: `access_token`/`id_token` stay in the encrypted JWT only; browser `session`
  exposes `user.{name,email,role}` only.
- D-D: split `auth.config.ts` (edge-safe) consumed by `auth.ts` + middleware.

**Changes (this repo):**
- C1 `src/auth.config.ts` — `NextAuthConfig`: `trustHost`, jwt session strategy,
  OIDC provider (`id:'furchert-ch'`, issuer, clientId/secret from env, scope
  `openid profile email`, `checks:['pkce','state']`); `jwt` callback copies
  `account.access_token`/`id_token` + `profile.role`; `session` callback exposes
  `user.role` only.
- C2 `src/auth.ts` — `NextAuth(authConfig)` → `{handlers,auth,signIn,signOut}`.
- C3 `src/app/api/auth/[...nextauth]/route.ts` — `export const {GET,POST}=handlers`.
- C4 `src/types/next-auth.d.ts` — augment `Session.user.role`, `JWT`
  (`role`,`accessToken`,`idToken`).
- C5 `src/middleware.ts` — compose next-intl with `NextAuth(authConfig).auth`
  wrapper (session resolution); matcher unchanged.
- C6 `src/components/Providers.tsx` (`'use client'`) — `<SessionProvider>`; mounted
  in `[locale]/layout.tsx`.
- C7 `src/components/layout/Header.tsx` — `authed` from `useSession()` (markup/
  styling unchanged).
- C8 `src/app/[locale]/dashboard/page.tsx` — RSC, `dynamic='force-dynamic'`;
  `await auth()`; unauth → `<SignInGate/>`, authed → placeholder (name/role +
  `<SignOutButton/>`).
- C9 `src/app/[locale]/dashboard/SignInGate.tsx` (`'use client'`) — prototype card
  styling, single real button → `signIn('furchert-ch',{redirectTo:`/${locale}/dashboard`})`.
- C10 `src/app/[locale]/dashboard/SignOutButton.tsx` (`'use client'`) — `signOut()`
  then redirect to `…/connect/logout?post_logout_redirect_uri=<origin>`.
- C11 `src/i18n/messages/{de,en}.json` — new `dashboard.*` labels; fix stale
  "Cloudflare Access" copy.
- C12 `.env.local.example` (committed, no values): `AUTH_SECRET`,
  `OIDC_CLIENT_ID=furchert-ch`, `OIDC_CLIENT_SECRET`, optional `AUTH_URL`.
- C13 `package.json` — pin exact `next-auth` beta (record resolved version here).

**Cross-repo (prepare+document, user applies):** `../auth-service`
`application.yaml` furchert-ch client (mirror grafana; redirect
`…/api/auth/callback/furchert-ch` prod+localhost; post-logout `https://furchert.ch`
+ localhost) + `k8s/deployment.yaml` env `FURCHERT_CH_CLIENT_SECRET` via
`secretKeyRef`; **psql seed** note (JDBC-backed, §6); secret via SOPS by user.

**Tests:** `pnpm lint`/`typecheck`/`build`; build output shows
`/[locale]/dashboard` dynamic `ƒ`, public routes still `●`/`○`; build succeeds
without secrets.

**Validation:** manual round-trip after user registers client + `.env.local`:
unauth `/de/dashboard` → gate → `auth.furchert.ch` → authed placeholder w/ role;
Header Homelab blue; sign-out clears session + hits `/connect/logout`. Greps: no
token in client bundle; no committed secret values.

**Risks:** next-intl×v5 middleware compose finicky (mitigate: split config +
page-gate; fallback intl-only middleware); `auth()` in layout kills SSG (avoided);
beta dep (pin exact + MEMORY note).

**Plan summary (3 bullets):**
- Auth.js v5 generic OIDC → auth.furchert.ch; page-level RSC gate; tokens
  server-only; Header via client session; public SSG preserved.
- Cross-repo auth-service client prepared as a ready-to-apply diff + psql note;
  user provisions secret.
- Verified by build/lint/typecheck + dynamic/static split; full round-trip is a
  documented user-setup manual step.

---

## 3. review

**plan-reviewer invoked:** yes — project type unavailable; ran the verbatim
`.claude/agents/plan-reviewer.md` persona via `general-purpose`. Verdict:
**PASS WITH NOTES**. context7 (`/nextauthjs/next-auth`) consulted for v5 patterns;
versions stay pinned.

**Findings & resolutions:**

| ID | Severity | Finding | Resolution (plan updated) |
|----|----------|---------|---------------------------|
| F1 | high | jwt callback reading `profile.role` is fragile: `profile` only present on first sign-in; no fallback/default → role can be lost or undefined | Add a provider **`profile()` callback** that maps `{ id:sub, name, email, role: profile.role ?? 'USER' }` (fail-closed default). jwt callback: `if (user) token.role = user.role`; `if (account) { token.accessToken = account.access_token; token.idToken = account.id_token; }`. (F-Q4 adopted.) |
| F2 | med | TS strict needs **both** `next-auth` and `next-auth/jwt` module augmentation; `Session.user` must extend `DefaultSession['user']` | C4: augment both modules; `interface Session { user: { role?: 'USER'\|'ADMIN' } & DefaultSession['user'] }`; `interface JWT { role?; accessToken?; idToken? }` |
| F3 | med | Custom inline provider does NOT auto-read `AUTH_*` env; must wire `process.env` explicitly. v5 uses `AUTH_SECRET` (commands.md/CONTRIBUTING still say `NEXTAUTH_SECRET`) | C1 explicit `clientId: process.env.OIDC_CLIENT_ID`, `clientSecret: process.env.OIDC_CLIENT_SECRET`; env secret `AUTH_SECRET`. Ship: update `commands.md`/CONTRIBUTING to the v5 name. |
| F4 / F-Q2 | med | Wrapping middleware in v5 `auth` adds the next-intl-compose risk for **no gain this phase** (D-A makes the page the gate; no protected routes for middleware to guard yet) | **C5 changed: middleware stays next-intl-only this phase.** Page-level `auth()` is the gate. Edge gating revisited in Phase 6 when real `/dashboard/*` admin routes exist. Removes the highest-risk item. |
| F5 | info(sec) | Page-gate is sufficient only because tokens never reach client (D-C) and there are no data routes yet | Added invariant: **every future data-bearing route/handler re-authorizes server-side (`role==='ADMIN'`); middleware refresh is never authorization.** |
| F6 / F-Q1 | high | Client-built `/connect/logout?...` cannot include `id_token_hint` without exposing the id_token to the browser → conflicts with D-C; SAS RP-logout typically needs the hint | **C10 redesigned:** federated logout is a **server Route Handler** `src/app/api/federated-logout/route.ts` that reads the id_token via `getToken` (server-side), builds `…/connect/logout?id_token_hint=…&post_logout_redirect_uri=<origin>`, clears the session cookie(s) on the redirect Response. `SignOutButton` just navigates to it. id_token stays server-only. |
| F7 | low | `SignInGate` must use the (fixed) `dashboard.gatesub` i18n key, not re-introduce the prototype's hardcoded "Cloudflare Access"; prototype gate is email/**GitHub** (not Google) | C9/C11 noted; gate copy via next-intl only |
| F8 | low | Exact beta pin deferred to install | Acceptance check: `grep next-auth package.json` shows full `5.0.0-beta.NN` (no range/dist-tag) |
| F9 | med | Confirm `<SessionProvider>`+`useSession()` keeps public pages static; Header must tolerate loading/unauth without layout shift | Keep layout `auth()`-free (D-B); build-output check (dashboard `ƒ`, rest `●`/`○`) is a required gate; Header treats non-`authenticated` as logged-out (no skeleton needed — same markup) |
| F-Q3 | — | Fail-closed role default | `role` defaults to `'USER'`; Phase-6 admin checks must be `role === 'ADMIN'` (never `!== 'USER'`) |
| F-Q5 | med | Don't hardcode `checks` and accidentally drop `nonce` SAS may require | **Do not set `checks`** on the provider — let Auth.js derive from discovery (PKCE included; SAS advertises `code_challenge_methods_supported`). Verify nonce/pkce during the manual round-trip; only override if discovery needs it. |
| F-Q6 | — | Cross-repo diff must mirror **grafana** (no `grant-types`, no `client_credentials`, no `clients:admin` scope), `{noop}`-prefixed secret, and include the **psql seed** note (JDBC-backed, won't seed a running instance) | Confirmed in the prepared diff |
| F-Q7 | — | Redirect-URI segment must equal provider id `furchert-ch` → `/api/auth/callback/furchert-ch` | Confirmed prod + localhost |

**Plan updates applied:** C1 (add `profile()` callback + explicit env wiring + no
hardcoded `checks`), C4 (dual-module augmentation), **C5 (middleware stays
intl-only — drop the `auth` wrapper this phase)**, **C10 (federated logout →
server Route Handler `src/app/api/federated-logout/route.ts`; `SignOutButton`
navigates to it)**, plus the fail-closed role default and the server-side
re-authorization invariant. The plan file's Phase-4 section is updated to match.

**Review summary (3 bullets):**
- Two high-severity items resolved: robust fail-closed role capture via a
  `profile()` callback, and federated sign-out moved server-side (keeps the
  id_token off the client, satisfies D-C while sending `id_token_hint`).
- Middleware `auth` wrapper dropped this phase (page-level gate is authoritative)
  — eliminates the plan's highest-risk item with zero loss of function.
- Plan implementation-ready; PASS WITH NOTES → all notes actioned.

---

## 4. implement

**Dependency:** `next-auth` pinned **exact** `5.0.0-beta.31` (no range) via
`pnpm add next-auth@beta` then verified `grep next-auth package.json`.

**Changes made (this repo):**
- C1 `src/auth.config.ts` — edge-safe `NextAuthConfig`; OIDC provider
  `furchert-ch` (issuer discovery, env clientId/secret, scope `openid profile
  email`, **no hardcoded `checks`** — derived from discovery, F-Q5);
  `profile()` maps `role` fail-closed to `'USER'` (F1/F-Q3); `jwt` persists
  `role`+`access_token`+`id_token`; `session` exposes `role` only (D-C).
- C2 `src/auth.ts`; C3 `src/app/api/auth/[...nextauth]/route.ts`.
- C4 `src/types/next-auth.d.ts` — augments `next-auth` (Session/User) **and**
  `next-auth/jwt` (F2).
- C5 `src/middleware.ts` — **unchanged** (intl-only, F-Q2).
- C6 `src/components/Providers.tsx` (`SessionProvider`) mounted in
  `[locale]/layout.tsx` (layout stays `auth()`-free → SSG preserved, D-B).
- C7 `src/components/layout/Header.tsx` — `authed` from `useSession()`.
- C8 `src/app/[locale]/dashboard/page.tsx` — RSC, `force-dynamic`,
  `robots:noindex`; `await auth()` gate → `<SignInGate/>` or placeholder
  (name/role + `<SignOutButton/>`).
- C9 `SignInGate.tsx` — prototype card styling, real `signIn('furchert-ch',
  {redirectTo})`; copy via next-intl (no hardcoded Cloudflare string).
- C10 `src/app/api/federated-logout/route.ts` (server) + `SignOutButton.tsx`
  (navigates to it) — RP-logout with `id_token_hint` server-side, clears
  session cookie (F6/F-Q1).
- C11 `src/i18n/messages/{de,en}.json` — real `dashboard.*` labels; removed
  the fake `signin`/`email`/`continue` keys; fixed the "Cloudflare Access" copy.
- C12 `.env.local.example` (committed, no values); real `.env.local` gitignored.
- C13 `package.json` — `next-auth: 5.0.0-beta.31`.

**Commands & results:**
```bash
$ pnpm add next-auth@beta        # → 5.0.0-beta.31 (pinned exact)
$ rm -rf .next && pnpm build     # pass (stale Phase-3 .next/types cleared)
$ pnpm lint                      # pass — no warnings/errors
$ pnpm typecheck                 # pass (via build; clean after .next reset)
```
Build: `/[locale]/dashboard` is **not** prerendered (no `de/dashboard.html`,
no prerender-manifest entry → dynamic despite the `●` summary glyph);
`/api/auth/[...nextauth]` + `/api/federated-logout` are `ƒ`; all public routes
remain `●`/`○` (SSG intact).

**Runtime smoke (temp `AUTH_SECRET`, never committed):**
- `/de`, `/de/about`, `/en/projects` → 200 (SSG).
- `/de/dashboard` (unauth) → 200, renders real gate ("Zugang beschränkt",
  "Mit furchert.ch anmelden"); **no** "Cloudflare" text.
- `/api/auth/providers` → `furchert-ch` OIDC, `callbackUrl …/api/auth/
  callback/furchert-ch` (matches cross-repo redirect-uri, F-Q7).
- `/api/federated-logout` → 307 → `https://auth.furchert.ch/connect/logout?
  post_logout_redirect_uri=http://localhost:3000` (id_token_hint added when a
  session exists).
- `grep idToken|accessToken .next/static` → empty (tokens never shipped, D-C).

**Implementation summary (3 bullets):**
- Real Auth.js v5 OIDC wired to auth.furchert.ch; page-level RSC gate; tokens
  server-only; Header reflects live session; SSG preserved for public pages.
- Federated sign-out done server-side with `id_token_hint`; middleware left
  intl-only this phase.
- All gates green; full login round-trip is a documented user-setup step
  (register client + `.env.local`).

---

## 5. check implementation

**requesting-code-review invoked:** pending (Phase-5 dispatch).

**Verification against plan:**
- [x] C1–C13 complete; C5 intentionally unchanged (F-Q2)
- [x] `pnpm lint`/`typecheck`/`build` pass; public pages SSG intact.
  **Build-marker note (corrects the earlier "dashboard ƒ" expectation):** Next
  labels `/[locale]/dashboard` `●` (not `ƒ`) because the parent
  `[locale]/layout.tsx` has `generateStaticParams`; however `force-dynamic`
  prevents any prerender — verified **no** `*dashboard*.html` in `.next` and no
  prerender-manifest entry, so `auth()` runs per request (functionally dynamic,
  no leaked content). The planned gate ("dashboard `ƒ`") is met in substance,
  not in glyph.
- [x] Unauth `/dashboard` renders real gate; providers + callback URL correct;
  federated logout redirects to IdP end-session
- [x] Tokens never reach client bundle (D-C); `.env.local.example` has no values;
  no secret/age/.sops files touched
- [x] Pinned exact `next-auth@5.0.0-beta.31`

## 6. ship

**code-review:** With fixes (1 Important + 2 Minor) — all applied:
- (Important) Chunked session cookies could survive logout when the JWT grows
  past ~4 KB → **stopped persisting `access_token`** (only `id_token`, needed
  for the logout hint; Phase 6 reintroduces it with chunk-aware handling) AND
  the logout route now also expires `${cookie}.0…4` variants. JWT augmentation
  trimmed accordingly.
- (Minor) `profile()` role mapping hardened to explicit equality
  (`=== 'ADMIN' ? 'ADMIN' : 'USER'`).
- (Minor) Worklog §5 build-marker wording corrected (dashboard `●` vs `ƒ`).
Re-verified after fixes: `pnpm typecheck`/`lint`/`build` all pass; dashboard
still not prerendered; `/api/auth/[...nextauth]` + `/api/federated-logout` `ƒ`.

**doc-auditor:** equivalent inline (project agent type unavailable).

**Docs updated:**
- [x] `README.md` — phase row 4 → done
- [x] `INTERFACES.md` — §1 marked implemented; end-session row; token-confinement
  + access-token-deferral notes
- [x] `DEPLOYMENT.md` — `AUTH_SECRET`/`AUTH_URL` (v5) secret table; ready-to-apply
  `../auth-service` client diff + JDBC `psql` seed note
- [x] `CONTRIBUTING.md` — local OIDC via `.env.local.example` + `AUTH_SECRET`;
  federated logout note
- [x] `.claude/rules/commands.md` — `NEXTAUTH_SECRET` → `AUTH_SECRET`; psql note
- [x] `OVERVIEW.md` — `/dashboard` auth-live (Phase 4) status + token note
- [x] `CHANGELOG.md` — Phase 4 entry

**Decision log:**

| ID | Decision | Rationale |
|----|----------|-----------|
| D1 | Auth.js v5 (`5.0.0-beta.31`, pinned) | App-Router-native; user-approved; only new dep |
| D2 | Page-level RSC `auth()` is the gate; middleware intl-only | RSC/route handlers must check server-side regardless; avoids next-intl×v5 compose risk this phase (F-Q2) |
| D3 | Tokens server-side only; persist only `id_token` now | Confine secrets (D-C) + avoid cookie chunking; `access_token` deferred to Phase 6 |
| D4 | Federated logout as a server route | Send `id_token_hint` without exposing the token (F6) |
| D5 | Header auth via client `SessionProvider`/`useSession` | Keep public pages SSG (no `auth()` in layout, D-B) |
| D6 | Fail-closed role via explicit equality | A missing/forged role can never become ADMIN |
| D7 | Cross-repo client = prepare+document | auth-service has its own workflow; secret is user-provisioned via SOPS |

**Final summary (3 bullets):**
- Real OIDC dashboard auth shipped (Auth.js v5); tokens stay server-side; public
  SSG preserved; federated sign-out ends the IdP session; build/lint/typecheck
  green.
- Verified locally (gate renders, providers/callback correct, logout redirect);
  full round-trip is a documented user-setup step.
- Plan-review (PASS WITH NOTES) + code-review (With fixes) fully actioned;
  middleware left intl-only; access-token + admin gating deferred to Phase 6.

**User actions required:**
- Register the `furchert-ch` client in `../auth-service` (apply the DEPLOYMENT.md
  diff + JDBC `psql` seed), provision `furchert-ch-client-secret` via SOPS, create
  `.env.local` (`AUTH_SECRET`, `OIDC_CLIENT_SECRET`), then `git commit` Phase 4.
- Suggested commit: `feat: real OIDC dashboard auth (Auth.js v5) (Phase 4)`.

**Memory entry added at top of `.claude/memory/MEMORY.md`:** yes
